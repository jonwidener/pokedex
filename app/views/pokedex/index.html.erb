<div class="">
  <div class="rounded-lg bg-red-600 shadow-lg p-4">
    <div class="text-xl">Pokédex</div>
    <div class="mb-4">
      <%= form_tag 'pokedex/search', :method => 'post' do %>
        <%= text_field_tag 'pokesearch' %>
      <% end %>
    </div>
    <% (1..150).each do |id| %>
      <div>
        <%= link_to 'pokemon/' + id.to_s do %><%= id.to_s.rjust(3, '0') %>.<%= @pokelist[id].upcase %><% end %>
      </div>
    <% end %>
  </div>
</div>
